# Compiler files
cache/
out/

# Forge gas snapshots (already-tracked files will still show modifications;
# run `git rm --cached -r .forge-snapshots/` to fully untrack them).
.forge-snapshots/

lcov.info

# Ignore Forge broadcast artifacts
**/broadcast/

# Ignore Forge gas snapshots
.forge-snapshots/

# Dotenv file
.env

# python virtualenv
env/

node_modules/
